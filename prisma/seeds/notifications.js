/**
 * 建立 Demo 通知資料。
 *
 * TODO: 依目前 notificationService 支援的事件重新規劃通知情境。
 */
export async function seedNotifications(
  _prisma,
  { users: _users, friendships: _friendships, activities: _activities },
) {}
